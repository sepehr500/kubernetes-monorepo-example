version: '3.2'
volumes:
    database_data:
        driver: local
services: 
  server:
    image: sepehr500/mono-server-example:latest
    build: ./server
    volumes: 
      - ./server/src:/app/src 
    ports:
      - "8080:8080"
